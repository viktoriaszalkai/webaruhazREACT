import React, { useContext } from 'react'
import { KosarContext } from '../contexts/KosarContext';

function Kosar() {
    const {kosaram} = useContext(KosarContext);
  return (
    
  )
}

export default Kosar