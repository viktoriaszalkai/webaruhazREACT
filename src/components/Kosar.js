import React, { useContext } from 'react'
import { KosarContext } from '../contexts/KosarContext';

function Kosar(props) {
   /*  const {kosarL} = useContext(KosarContext);
    return(
    kosarL.map((termek)=>{
        return <div>{termek.title}</div>
    })
    ) */
}

export default Kosar